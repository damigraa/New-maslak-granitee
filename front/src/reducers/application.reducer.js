import { applicationConstants } from './../actions/constants';

const initState = {
  applications: []
};
export default (state = initState, action) => {
  switch (action.type) {

  }

  return state;
};
 